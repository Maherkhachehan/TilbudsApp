<Page
    x:Class="TilbudsApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ViewModel="using:TilbudsApp.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:TilbudsApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>

        <ListView
            Width="556"
            Height="600"
            Margin="80,252,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Background="Navy"
            Foreground="Black"
            ItemsSource="{x:Bind ViewModel.ButikSingleton.ButikCollection, Mode=OneWay}" />

        <ListView
            Width="602"
            Height="600"
            Margin="828,252,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Background="Navy"
            ItemsSource="{x:Bind ViewModel.ButikSingleton.ByerCollection, Mode=OneWay}" />
        <!--<TextBlock x:Name="Byer" HorizontalAlignment="Left" Margin="236,154,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="256" Height="98" SelectionChanged="TextBlock_SelectionChanged" FontSize="72" Text="Byer">

        </TextBlock>-->

    </Grid>

</Page>
